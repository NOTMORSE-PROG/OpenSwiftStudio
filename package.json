{
  "name": "openswiftstudio",
  "version": "0.0.1",
  "description": "Open-source standalone IDE for iOS development on Windows. No Mac required.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "comments": "We use npm by default. pnpm/yarn work too — adjust beforeDev/beforeBuild in src-tauri/tauri.conf.json if you switch.",
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-http": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@vscode/codicons": "^0.0.36",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.3.2",
    "vite-plugin-solid": "^2.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
