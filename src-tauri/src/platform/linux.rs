// Linux-specific platform implementations — stub per ADR-006.
// Linux support arrives in v0.7 (provisional). Architecture stays clean from M0
// so the eventual port is 4-6 weeks instead of a rebuild.
